{
  "name": "@brika/db",
  "version": "0.1.0",
  "description": "Database layer and migration system for Brika",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "brika-db": "./bin/brika-db.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test --passWithNoTests",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@brika/cli": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.10"
  }
}
