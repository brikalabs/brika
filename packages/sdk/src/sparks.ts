/**
 * @brika/sdk/sparks
 *
 * Typed events (sparks) — define, emit, and subscribe.
 */

export type { CompiledSpark } from './api/sparks';
export { defineSpark, subscribeSpark } from './api/sparks';
export { z } from './blocks/schema';
