{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // TODO(typecheck): existing tests don't satisfy `noUncheckedIndexedAccess`.
    // Flip back to `true` once the index-access patterns in
    // `actions-client.test.ts` and `validate.test.ts` are tightened.
    "noUncheckedIndexedAccess": false
  },
  "include": ["src"]
}
