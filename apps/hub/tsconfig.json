{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["ES2024"],
    "paths": {
      "@/*": ["./src/*"]
    },
    // TODO(typecheck): existing hub code does not satisfy
    // `noUncheckedIndexedAccess`. Flip to `true` once the route + persistence
    // layers are audited.
    "noUncheckedIndexedAccess": false
  },
  "include": ["src", "compiler"]
}
