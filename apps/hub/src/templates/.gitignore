# BRIKA runtime files — generated and managed by the hub, never committed.
#
# Everything the hub owns (databases, installed plugins, identity, update state,
# secrets, caches) lives under .system/. Only the human-authored files stay
# tracked: brika.yml, boards/ and workflows/.
/.system/

# OS files
.DS_Store
Thumbs.db
