{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "composite": true,
    "jsxImportSource": "react",
    // TODO(typecheck): `src/main.ts:157` doesn't satisfy
    // `noUncheckedIndexedAccess`. Flip back to `true` once tightened.
    "noUncheckedIndexedAccess": false
  },
  "include": ["src"],
  "exclude": ["src/blocks/**/*.view.tsx", "src/blocks/**/*.node.tsx"]
}
